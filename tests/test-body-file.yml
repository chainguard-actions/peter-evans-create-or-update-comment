name: Test comment creation with deprecated body-file input
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    env:
      GITHUB_REF: refs/heads/main
      GITHUB_EVENT_NAME: push
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Create comment body file
        run: |
          mkdir -p /tmp/comment-test-body-file
          echo "This is a test comment from a file using the deprecated body-file input." > /tmp/comment-test-body-file/body.md
          echo "It has multiple lines." >> /tmp/comment-test-body-file/body.md
          cat /tmp/comment-test-body-file/body.md

      - name: Create comment using deprecated body-file input
        id: create-comment
        uses: .
        with:
          token: ${{ github.token }}
          issue-number: 1
          body-file: /tmp/comment-test-body-file/body.md
        continue-on-error: true

      - name: Assert step ran (output or error)
        env:
          COMMENT_ID: ${{ steps.create-comment.outputs.comment-id }}
          OUTCOME: ${{ steps.create-comment.outcome }}
        run: |
          echo "outcome: $OUTCOME"
          echo "comment-id: $COMMENT_ID"
          if [ "$OUTCOME" = "success" ]; then
            if [ -z "$COMMENT_ID" ]; then
              echo "FAIL: action succeeded but comment-id output is empty"
              exit 1
            fi
            echo "PASS: comment created with id=$COMMENT_ID"
          else
            echo "INFO: action did not succeed (expected under act without real GitHub token)"
            echo "PASS: action invoked correctly with body-file input, API failure is expected in test environment"
          fi
